import { Job } from "../../typings/jobs.type";

type WorkerJobsHistoryProps = {
  jobs: Job[];
};

const WorkerJobsHistory = ({ jobs }: WorkerJobsHistoryProps) => {
  return <div>WorkerJobsHistory</div>;
};

export default WorkerJobsHistory;
