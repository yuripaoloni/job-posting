import { Jobs } from "../../typings";

type ManagerJobsHistoryProps = {
  jobs: Jobs[];
};

const ManagerJobsHistory = ({ jobs }: ManagerJobsHistoryProps) => {
  return <div>ManagerJobsHistory</div>;
};

export default ManagerJobsHistory;
